{
  "name": "erc20-faucet",
  "version": "1.0.0",
  "description": "ERC-20 Token Faucet DApp with rate limiting and wallet integration",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "keywords": ["blockchain", "ethereum", "erc20", "faucet"],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomiclabs/hardhat-verify": "^2.0.0",
    "hardhat": "^2.20.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.3.1"
  }
}
